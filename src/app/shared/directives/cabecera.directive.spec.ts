import { CabeceraDirective } from './cabecera.directive';

describe('CabeceraDirective', () => {
  it('should create an instance', () => {
    const directive = new CabeceraDirective();
    expect(directive).toBeTruthy();
  });
});
